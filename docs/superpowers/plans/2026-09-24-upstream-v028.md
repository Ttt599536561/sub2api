# Upstream v0.2.8 Integration Plan

> For agentic workers: use subagent-driven-development for bounded implementation and independent review. Follow the checkboxes below.

**Goal:** Integrate official v0.2.8 and its official VERSION synchronization into the committed custom branch, preserve upstream functionality and logic, obtain three consecutive review rounds, then push and publish a verified dual-architecture image.

**Architecture:** Three-way merge from `713d2852e8ea8addce365f595d95823d26bb5aa2` and `upstream/main` (`a3eb7ef30`, official release plus VERSION-only synchronization). Preserve official behavior and adapt custom monthly resets, welfare and session isolation around it. All uncommitted original workspace changes are excluded.

**Tech Stack:** Go 1.27, Vue/TypeScript, PostgreSQL/Redis integration tests, GitHub Actions, GHCR.

## 1. Inventory and baseline

- [x] Confirm origin/upstream, release v0.2.8, merge base `aea725f2e`, 26 custom / 238 upstream commits.
- [x] Preserve original dirty-workspace patch and file/status inventory outside repository.
- [x] Create isolated `codex/merge-upstream-v0.2.8` worktree.
- [x] Record all upstream/custom file deltas and their overlap in an audit report.
- [x] Run targeted pre-merge tests for custom subscription/welfare and frontend conflict areas.

## 2. Prepare merge without committing

- [x] Run `git merge --no-commit --no-ff upstream/main` in the isolated worktree.
- [x] Resolve `.gitignore` by retaining applicable documentation allowlists; take official `backend/cmd/server/VERSION`.
- [x] Resolve `backend/cmd/server/wire_gen.go` retaining all new upstream services/setters and custom service lifecycle hooks.
- [x] Resolve `backend/internal/service/billing_cache_service.go` and `gateway_usage_billing.go` from upstream behavior, adapting custom reset/welfare hooks. Keep official simple-mode spending-window enforcement and new pricing behavior.
- [x] Resolve `backend/internal/service/redeem_service.go` in favor of official locking and remaining-time semantics; remove redundant custom locking.
- [x] Resolve frontend `BaseDialog.vue` with official scroll locking and custom focus/inert behavior, `announcements.ts` with official fetch failure propagation and custom session guards, and `GroupsView.duplicate.spec.ts` with both valid assertions.
- [x] Review every automatically merged overlap against both parents, including DTO/routes/Wire and gateway admission/usage.
- [x] Keep all historical custom migration contents unchanged; update the custom upstream-baseline upgrade test only after auditing new upstream migration names/checksums.

## 3. Validate and review

- [x] Run frontend full Vitest, ESLint, vue-tsc and production Vite build.
- [ ] Run backend complete unit tests, required integration tests with `CI=true`, golangci-lint and embedded backend build.
- [ ] Run required deployment shell checks and release-tool tests.
- [x] Review round 1: independent backend/upstream semantics, frontend/custom compatibility, migrations/build/release. Fix every blocking finding.
- [x] Review round 2: fresh independent review of corrected complete merge; verify round-1 resolutions and upstream preservation.
- [x] Review round 3: final independent review against frozen tree and test evidence. Any subsequent code fixes require targeted verification and reviewer confirmation.
- [x] Verify no unmerged entries, accidental conflict markers, unexpected deletions, or excluded workspace edits.

## 4. Integrate and publish

- [ ] Create the merge commit only after review acceptance, preserve original branch/dirty files, then fast-forward the remote feature branch after exact-commit CI while keeping the original dirty checkout unchanged.
- [ ] Push the reviewed source to fork; verify GitHub CI and Security Scan for that exact commit.
- [ ] Adapt custom-only image workflow to version `0.2.8-r1`, pin the immutable event SHA (the already-reviewed source commit), and preserve native amd64/arm64 smoke checks and immutable final tags.
- [ ] Push the publication branch after release configuration review; wait for successful GitHub builds.
- [ ] Verify GHCR manifest, both architectures, source revision, public pull access and immutable digest.
- [ ] Deliver update summary, conflict decisions, three-round review evidence, checks and deployable image reference. Do not deploy the server.
